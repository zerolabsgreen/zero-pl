import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  popoverText: {
    fontSize: '12px',
    fontFamily: 'Rajdhani',
    fontWeight: 600,
    textAlign: 'center',
  },
});
